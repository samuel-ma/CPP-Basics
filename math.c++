

#include <iostream>
#include <string>
#include <cmath>
using namespace std;

int main(){

  // Max and Min
  // The max(x,y) function can be used to find the highest value of x and y:
  cout << sqrt(64);
  cout << round(20.5);
  cout << log(10);  
  return 0;

}